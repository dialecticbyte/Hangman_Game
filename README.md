# Hangman_Game
This is primarily a word game using Javascript, the script randomly takes in a word from a JSON library, the user has to guess the word, if the user fails the there is observed a hangman on the screen.
